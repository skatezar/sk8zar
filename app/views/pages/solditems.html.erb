<% if current_user.admin == true %>
  <div class="container">
    <% @items.each do |item| %>
      <ul>
        <li>
          <%= link_to item.title, item_path(item), class: "btn btn-new" %>
        </li>
        <br>
        <br>
      </ul>
    <% end %>
  </div>
<% end %>
